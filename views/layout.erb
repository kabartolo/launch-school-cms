<!DOCTYPE html>
  <head>
    <title>CMS</title>
    <link rel="stylesheet" href="/cms.css">
  </head>
  <body>
    <% if session[:message] %>
      <p class="message"><%= session.delete(:message) %></p>
    <% end %>

    <%= yield %>
  </body>
</html>